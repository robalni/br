#include <stdio.h>

#define LOG_E(msg) \
    puts(msg)

#define LOG_W(msg) \
    LOG_E(msg)

#define LOG_I(msg) \
    LOG_E(msg)

#define SHOW_E(msg) \
    puts(msg)

#define SHOW_W(msg) \
    SHOW_E(msg)

#define SHOW_I(msg) \
    SHOW_E(msg)
